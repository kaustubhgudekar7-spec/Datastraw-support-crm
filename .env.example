# SQLite is used by default and needs no configuration.
# To point at a different database (e.g. Postgres on Railway), set:
# DATABASE_URL=postgresql://user:password@host:port/dbname
DATABASE_URL=sqlite:///./support_crm.db

# Port the app binds to (Railway sets this automatically in production)
PORT=8000
